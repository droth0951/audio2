{
  "name": "podknowledge-wrapper",
  "version": "1.0.0",
  "description": "Railway deployment wrapper for Audio2 server",
  "scripts": {
    "start": "cd audio-trimmer-server && npm start",
    "install": "cd audio-trimmer-server && npm install"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {}
}