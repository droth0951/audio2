[build]
builder = "nixpacks"

[deploy]
startCommand = "cd audio-trimmer-server && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
